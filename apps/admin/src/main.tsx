import React, { useState } from "react";
import { createRoot } from "react-dom/client";
import "./styles.css";

const nav = ["AI Command Center", "Overview", "Orders", "Products", "Inventory", "Customers", "Marketing", "Sales Channels", "Website", "POS", "B2B", "Suppliers", "Finance", "Analytics", "Automations", "AI Employees", "Settings"];
const agents = [
  { name: "Sales Agent", purpose: "Recover high-value abandoned carts", status: "Watching", metric: "42 carts", action: "Review recovery" },
  { name: "Inventory Agent", purpose: "Detect stock risk and prepare replenishment", status: "Watching", metric: "7 SKUs", action: "Review reorder" },
  { name: "Marketing Agent", purpose: "Find conversion opportunities", status: "Ready", metric: "3 insights", action: "View insights" },
  { name: "Finance Agent", purpose: "Explain margin and anomaly signals", status: "Ready", metric: "2 alerts", action: "View alerts" },
];

function App() {
  const [active, setActive] = useState("AI Command Center");
  const [approved, setApproved] = useState<Record<string, boolean>>({});
  const [toast, setToast] = useState<string | null>(null);
  const show = (message: string) => { setToast(message); window.setTimeout(() => setToast(null), 2200); };
  const approve = (id: string, label: string) => { setApproved(prev => ({ ...prev, [id]: true })); show(`${label} approved for agent execution`); };

  return <div className="app"><aside className="sidebar"><div className="brand"><span className="brand-mark">C</span><span>Commerce OS</span></div><div className="store">Demo Store <span>⌄</span></div><nav>{nav.map(item => <button className={item === active ? "nav active" : "nav"} key={item} onClick={() => setActive(item)}>{item}</button>)}</nav></aside><main className="main">
    <header className="topbar"><div><span className="eyebrow">AI COMMAND CENTER</span><h1>{active === "AI Employees" ? "AI Operations" : "Good afternoon"}</h1></div><button className="command" onClick={() => show("Command Center ready")}>Ask Commerce OS <kbd>⌘ K</kbd></button></header>
    {active === "AI Employees" ? <section className="content" style={{ marginTop: 24 }}><div className="panel" style={{ gridColumn: "1 / -1" }}><div className="panel-head"><div><span className="eyebrow">AI EMPLOYEES</span><h2>Autonomous operations</h2></div><span>4 agents active</span></div><p>Agents surface opportunities and prepare actions. Nothing executes without the required permission or approval.</p></div><div className="grid" style={{ gridColumn: "1 / -1" }}>{agents.map(agent => <article key={agent.name}><span className="label">{agent.status.toUpperCase()}</span><strong>{agent.name}</strong><p>{agent.purpose}</p><small>{agent.metric} · human approval for actions</small><button className="primary" style={{ marginTop: 14 }} onClick={() => show(`${agent.name}: ${agent.action}`)}>{agent.action}</button></article>)}</div><div className="panel" style={{ gridColumn: "1 / -1" }}><div className="panel-head"><h2>Recent agent activity</h2><span>Live</span></div><div className="action"><div><b>Inventory Agent</b><p>Low-stock risk detected for 7 SKUs. Reorder proposals prepared.</p></div><button onClick={() => approve("inventory", "Inventory reorder")}>{approved.inventory ? "Approved" : "Review"}</button></div><div className="action"><div><b>Sales Agent</b><p>42 abandoned carts identified with an estimated ₹31,400 recovery opportunity.</p></div><button onClick={() => approve("sales", "Cart recovery")}>{approved.sales ? "Approved" : "Review"}</button></div></div></section> : <><section className="hero"><div><span className="eyebrow">BUSINESS HEALTH</span><div className="score">82<span>/100</span></div><p>Your store is healthy. AI has identified 3 actions worth reviewing.</p></div><button className="primary" onClick={() => show("Opening recommended actions")}>Review actions</button></section><section className="grid"><article><span className="label">Revenue</span><strong>₹4.82L</strong><small>+12.4% vs last week</small></article><article><span className="label">Orders</span><strong>1,284</strong><small>+8.7% vs last week</small></article><article><span className="label">Conversion</span><strong>3.84%</strong><small>+0.42 pts</small></article><article><span className="label">Profit</span><strong>₹1.31L</strong><small>27.2% margin</small></article></section><section className="content"><div className="panel"><div className="panel-head"><h2>AI recommendations</h2><span>3 actions</span></div><div className="action"><div><b>Inventory risk</b><p>Product A may stock out in 4 days.</p></div><button onClick={() => approve("inventory", "Inventory reorder")}>{approved.inventory ? "Approved" : "Reorder"}</button></div><div className="action"><div><b>Conversion opportunity</b><p>Mobile product page conversion fell 8%.</p></div><button onClick={() => show("Investigation opened")}>Investigate</button></div><div className="action"><div><b>Abandoned carts</b><p>42 carts could recover approximately ₹31,400.</p></div><button onClick={() => approve("sales", "Cart recovery")}>{approved.sales ? "Approved" : "Review"}</button></div></div><div className="panel"><div className="panel-head"><h2>AI employees</h2><span>Active</span></div>{agents.map(agent => <button className="agent" key={agent.name} onClick={() => setActive("AI Employees")}><span className="dot" />{agent.name}<span className="running">{agent.status}</span></button>)}</div></section></>}{toast && <div className="toast">{toast}</div>}</main></div>;
}

createRoot(document.getElementById("root")!).render(<React.StrictMode><App /></React.StrictMode>);
